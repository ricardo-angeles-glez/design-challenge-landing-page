{
  "name": "odoo-design-exercise",
  "version": "1.0.0",
  "scripts": {
    "sass": "sass scss/main.scss css/main.css --quiet-deps --no-source-map --quiet",
    "sass-watch": "sass --watch scss/main.scss css/main.css --quiet-deps --quiet",
    "sass-prod": "sass scss/main.scss css/main.css --style compressed --quiet-deps --quiet"
  },
  "devDependencies": {
    "bootstrap": "^5.3.2",
    "sass": "^1.97.1"
  }
}